version: '3'
services:
  redis:
    image: redis
    container_name: redis
    ports:
      - "6379:6379"
  django:
    build: .
    container_name: django_app
    ports:
      - "8000:8000"
    depends_on:
      - redis
